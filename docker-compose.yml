services:
    db:
        image: postgres:16
        container_name: audio-db
        environment:
            POSTGRES_DB: audio
            POSTGRES_USER: audio
            POSTGRES_PASSWORD: audio
        ports:
            - "5432:5432"
        volumes:
            - pgdata:/var/lib/postgresql/data
volumes:
    pgdata:
